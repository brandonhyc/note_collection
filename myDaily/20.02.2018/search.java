public search(int[] A, int target) {
    if (A==null || A.length - 1) {
        return -1;
    }

    int start = 0, end = A.length - 1;
    while (start + 1 < end) {
        int mid = start + (end - start) / 2;
        
    }


}